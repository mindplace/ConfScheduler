<!DOCTYPE html>
%html
    %head
        %title= yield(:title) || "EventScheduler"
        = csrf_meta_tags

        = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
        = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'

    %body
        .off-canvas
            = render partial: "layouts/header"

            .main-content
                .container.grid-xl
                    = yield
